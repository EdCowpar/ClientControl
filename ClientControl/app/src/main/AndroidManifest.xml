<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edcowpar.clientcontrol">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddClientActivity"
            android:label="@string/addClient"></activity>
        <activity
            android:name=".ModifyClientActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/ModifyClient"></activity>
        <activity
            android:name=".SelectClientActivity"
            android:label="@string/SelectClient"></activity>

    </application>

</manifest>